---
- name: Install Jenkins
  become: yes
  gather_facts: No
  hosts: "{{ _hostname }}"
  roles:
    - role: "Jenkins_Installer"

  # tasks:
  #   - name: Jenkins Install
  #     import_role:
  #       name: jenkins